### Enhancements

* <news item>

### Bug fixes

* Fix hardcoded Python paths in entry point scripts from `conda pypi convert`. Entry-point scripts are now handled exclusively via `info/link.json` (CEP-34), so conda generates them at install time with the correct prefix. (#309)

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* <news item>
